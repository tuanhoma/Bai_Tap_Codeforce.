#include<stdio.h>
#include<math.h>
int main(){
	int i;
	char a[i], c[i];
	for( i = 0; i < a,c ; i++ ){
		scanf("%c %c", &a, &c);
	}
	( 'a' <= a <= 'z');
	if( c >= 'A' && c <= 'Z'){
		c += 32;
		c == a;
		printf("0");
	}
	else if( a > c ){
		printf("1");
	}
	else if( a < c ){
		printf("-1");
	}
	return 0;
}